function testCycle(n) {
    x = 0;
       for(i=1;i<=n;i++)
       {x+=i;}
       return x;
   }

//Функция testCycle вычисляет сумму всех целых чисел от 1 до n (включительно) и возвращает это значение.